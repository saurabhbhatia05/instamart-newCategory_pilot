# Copy to .streamlit/secrets.toml for local Streamlit (gitignored).
# On Streamlit Cloud: paste the same keys in App settings → Secrets.

GROK_API_KEY = ""
USE_LLM = "false"
GROK_MODEL = "grok-2-latest"
SURVEY_PDF_PATH = "data/survey/responses.pdf"
APP_ENV = "production"
